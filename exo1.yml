openapi: 3.0.3
info:
  title: STD24059 - TD1
  version: 1.0.0
paths:
  /ping:
    get:
      responses:
        200:
          description: OK
          content:
            text/plain:
              schema:
                type: string
                example: pong